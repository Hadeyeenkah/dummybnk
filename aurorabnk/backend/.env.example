# Backend environment variables (example)
MONGODB_URI=mongodb://127.0.0.1:27017/securebank
JWT_SECRET=replace_this_with_a_strong_secret
JWT_REFRESH_SECRET=replace_refresh_secret
PORT=5001
DEMO_SEED=true
CLIENT_ORIGIN=http://localhost:3000
API_BASE=http://localhost:5001

# Optional
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
# Server Configuration
PORT=5000
NODE_ENV=development
API_BASE=http://localhost:5000

# Database Configuration (Local MongoDB or Atlas)
# Local: ensure a MongoDB server is running on 127.0.0.1:27017
# Atlas: paste your connection string below (remember to set user/pass)
# Examples:
# MONGODB_URI=mongodb://127.0.0.1:27017/securebank
# MONGODB_URI=mongodb+srv://<user>:<pass>@<cluster>.mongodb.net/securebank?retryWrites=true&w=majority
MONGODB_URI=mongodb://127.0.0.1:27017/securebank

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_min_32_characters_long_change_in_production
JWT_EXPIRE=15m
JWT_REFRESH_SECRET=your_refresh_token_secret_min_32_characters_long

# Encryption
ENCRYPTION_KEY=your_32_character_encryption_key_here_exactly_32_chars

# CORS
# You can specify one or many origins. The server already allows common dev origins
# (http://localhost:3000, 3001, 3002, 5173, and 127.0.0.1 variants), but you can add more here.
# Use CLIENT_ORIGIN for a single origin or CLIENT_ORIGINS for a comma-separated list.
CLIENT_ORIGIN=http://localhost:3001
# CLIENT_ORIGINS=http://localhost:3000,http://localhost:5173

# API Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Session Configuration
SESSION_SECRET=your_session_secret_key_here
